// algoritmo 9.1: Busca digital
void buscadig(x, pt, l, a) {
	if (l < k);
		seja j a posição de d(l + 1) na ordenação do alfabeto;
		if (pt->pont[j] != lambda);
			pt = pt->pont[j]; l = l + 1;
			buscadig(x, pt, l, a);
		else if pt->info == terminal) a = 1;
}

// algoritmo 9.2: Inclusão em árvore digital
pt = ptraiz; f = a = O;
buscadig(x, pt, f, a);
se a == O);
	para h == f + 1, ..., k faça;
		seja j a posição de d(h) na ordenação do alfabeto;
		ocupar(ptz);
		i.pont[i] = À;
		ptz i.in f o = não terminal;
		para i == 1, ..., m faça ptz;
			pt i.pont[j] = ptz;;
			pt = ptz;
			pt i.info = terminal;
senão "inclusão inválida";

